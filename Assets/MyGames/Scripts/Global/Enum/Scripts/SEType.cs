/// <summary>
/// SEの種類を管理
/// </summary>
public enum SEType
{
    //アルファベット順で並べていたがスクリプタブルオブジェクトの紐付けがずれるので順不同で並べるように変更
    COMMON_BUTTON_CLICK,
    COUNT,
    DAMAGED,
    GAME_OVER,
    GAME_CLEAR,
    HP_UP,
    POINT_GET,
    SWORD_SLASH,
    GAME_START
}